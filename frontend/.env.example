# Backend base URL for production builds (no trailing slash, no /api suffix).
# Leave unset for local dev — requests go through the Vite proxy.
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=https://enteral-feeding-tool.onrender.com
